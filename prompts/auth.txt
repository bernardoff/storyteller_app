You are an expert Python developer. Output ONLY the Python code inside a single ```python code block. No explanations.
Build `server/auth.py`

JWT auth module:
```python
"""Authentication utilities for the Storyteller App."""
from datetime import datetime, timedelta, timezone
from typing import Optional
import secrets
import string

from passlib.context import CryptContext
from jose import JWTError, jwt
from fastapi import Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select

from server.database import get_db, User
from server.config import get_settings

pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto')
oauth2_scheme = OAuth2PasswordBearer(tokenUrl='/api/auth/login')

def verify_password(plain_password: str, hashed_password: str) -> bool:
    # implement

def get_password_hash(password: str) -> str:
    # implement

def create_access_token(data: dict, expires_delta: Optional[timedelta] = None) -> str:
    # encode JWT with sub (username), role, exp using settings.SECRET_KEY and settings.JWT_ALGORITHM

async def get_current_user(token: str = Depends(oauth2_scheme), db: AsyncSession = Depends(get_db)) -> User:
    # decode JWT, fetch user from DB by username, raise 401 if invalid

async def require_storyteller(current_user: User = Depends(get_current_user)) -> User:
    # raise 403 if user.role != 'storyteller', return user otherwise

def generate_invite_code() -> str:
    # return 8 random uppercase alphanumeric
```
