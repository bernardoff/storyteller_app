You are an expert Frontend developer. Output ONLY HTML code inside a single ```html code block. No explanations.
Build `client/index.html` for the Storyteller App.
Requirements:
- Premium dark gothic theme (Vampire: The Dark Ages).
- Link to `style.css` and `app.js`.
- Two main sections: 
  1. Login/Setup Screen (username, password, login btn, setup btn)
  2. Dashboard Screen (hidden by default).
- Dashboard contains: 
  - Header: Welcome message & logout button.
  - Sidebar: Navigation (Chat, Dice, Character).
  - Main panel: A dice roller area (pool size, difficulty, roll button) and a feed area to display WebSocket broadcast messages.
Use semantic HTML and unique IDs for JS interaction.
